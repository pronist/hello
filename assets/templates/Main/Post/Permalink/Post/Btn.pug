.permalink__btn
  if process.env.NODE_ENV === 'development'
    button(type='button' class='btn_mark' role='report') 신고하기

    script.
      $(document).ready(() => {
        $('.permalink__btn button[role=report]').appendTo('#tistoryEtcLayer .bundle_post')
      })
