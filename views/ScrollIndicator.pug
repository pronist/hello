#scroll-indicator
  .progress-container
    .progress-bar

script.
  /* Scoll Indicator */
  $(document).ready(() => {
    $(window).on('scroll', () => {
      h.scrollIndicator('#scroll-indicator .progress-bar')
    })
  })
