#scroll-indicator
  .progress-container
    .progress-bar

script.
  /* Scoll Indicator */
  $(document).ready(() => {
    // Set 'Scroll Indicator'
    $(window).on('scroll', () => h.setScrollIndicator('#scroll-indicator .progress-bar'))
  })
