#scroll-indicator
  .progress-container
    .progress-bar

script.
  /* Scoll Indicator */
  $(document).ready(() => {
    $(window).on('scroll', () => {
      h.Layout.setScrollIndicator('#scroll-indicator .progress-bar')
    })
  })
