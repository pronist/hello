aside#sidebar(
  :class='sidebar ? "left-0" : "-left-[calc(theme(spacing.s))]"'
  class='h-full fixed top-0 w-s overflow-hidden z-50 transition-[left] duration-200 p-0 bg-h-200 dark:bg-h-700 <s_if_var_sidebar>xl:left-0</s_if_var_sidebar>')
  ul.wrap(class='flex flex-col gap-y-6 overflow-x-hidden overflow-y-scroll h-full w-[calc(theme(spacing.s)+theme(spacing.8))] absolute py-6 border-box')
    s_sidebar
      s_sidebar_element
        // 프로필
        include Sidebar/Profile
      s_sidebar_element
        // 방문자 수
        include Sidebar/Counter
      s_sidebar_element
        // 검색
        include Sidebar/Search
      s_sidebar_element
        // 카테고리
        include Sidebar/Category
      s_sidebar_element
        // 블로그 메뉴
        include Sidebar/BlogMenu
      s_sidebar_element
        // 링크
        include Sidebar/MyLinks
      s_sidebar_element
        // 공지사항
        include Sidebar/Notice
      s_sidebar_element
        // 인기 글
        include Sidebar/PopularPosts
      s_sidebar_element
        // 태그
        include Sidebar/RandomTags
      s_sidebar_element
        // 최근 댓글
        include Sidebar/RecentComments
      s_sidebar_element
        // 최근 글
        include Sidebar/RecentPosts
    include Sidebar/Copyright
  template(x-teleport='body')
    #mask(
      x-show='sidebar'
      x-transition:enter='transition ease-linear duration-300'
      x-transition:enter-start='opacity-0'
      x-transition:enter-end='opacity-100'
      x-transition:leave='transition ease-linear duration-300'
      x-transition:leave-start='opacity-100'
      x-transition:leave-end='opacity-0'
      @click='sidebar = false'
      class='fixed top-0 left-0 w-screen h-screen bg-h-900/50 backdrop-blur z-40 duration-200')

style
  :postcss
    #sidebar {
      .el {
        @apply w-s px-5;

        h2 {
          @apply text-h-800 mb-5 font-bold text-sm dark:text-h-200;
        }

        i {
          @apply text-h-400;
        }

        a:not(.link_tit) {
          @apply text-h-400 hover:text-h-800 dark:hover:text-h-200;
        }
      }
    }
