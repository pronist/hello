s_guest
  #guestbook(
    x-init='$store.header.title = "방명록"'
    x-ref='guestbook'
    class='w-full px-4 lg:px-0 lg:max-w-screen-lg lg:w-pem lg:mx-auto')
    +replyForm('guest')
    +replyList('guest', 'guest_reply')
