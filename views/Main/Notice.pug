s_notice_rep
  #__notice
    section.__list.main__list(data-mode='[##_var_notice-list-mode_##]')
      header.list__header
        .heading
          h1.title 공지사항
      ul
        +index('notice', false)
    +permalink('permalink_article', 'notice', false)

script.
  /* Notice */
  $(document).ready(() => {
    // Remove list element when 'Notice' permalink page
    $('#tt-body-page #__notice > .__list[data-mode]').remove()
  })
