#__loader
  div(class='la-ball-clip-rotate uk-position-center')
    each _ in Array(1)
      div

script.
  /* Loader */
  $(window).on('load', () => {
    // 'Loader' Out
    $('#__loader').fadeOut(500)
  })
