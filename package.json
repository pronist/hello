{
  "scripts": {
    "start": "cross-env NODE_ENV=development tidory start",
    "preview": "cross-env NODE_ENV=development tidory preview",
    "build": "cross-env NODE_ENV=build tidory build",
    "production": "cross-env NODE_ENV=production tidory production",
    "deploy": "tidory deploy",
    "store": "tidory store",
    "lint": "eslint --ext .js --ignore-path .gitignore . && pug-lint .",
    "github:user": "push-dir --dir=dist --branch=dist --cleanup"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/tistory-project/hELLO"
  },
  "devDependencies": {
    "cross-env": "^7.0.2",
    "eslint": "^6.8.0",
    "eslint-config-standard": "^14.1.1",
    "eslint-loader": "^4.0.0",
    "eslint-plugin-import": "^2.23.3",
    "eslint-plugin-node": "^11.1.0",
    "eslint-plugin-promise": "^5.1.0",
    "eslint-plugin-standard": "^5.0.0",
    "jstransformer-stylus": "^1.5.0",
    "pug-lint": "^2.6.0",
    "push-dir": "^0.4.1",
    "stylus": "^0.54.7",
    "stylus-loader": "^3.0.2"
  },
  "dependencies": {
    "lazysizes": "^5.3.0",
    "tidory": "latest"
  }
}
